[project]
name = "githubexperiments"
version = "0.1.0"
description = "Experiments with GitHub data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-duckdb>=1.10.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]